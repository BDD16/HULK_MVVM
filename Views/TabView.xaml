<UserControl x:Class="HULK.Views.TabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HULK.Views"
             xmlns:vm="clr-namespace:HULK.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="52" d:DesignWidth="300">
    <Grid>
        <Border BorderThickness="2" BorderBrush="#FF591C97" CornerRadius="3" >
            <Grid HorizontalAlignment="Left" Height="51" VerticalAlignment="Top" Width="300" Background="#FF1F2120">
                <Label x:Name="SettingsLabel" Content="Settings" HorizontalAlignment="Left" Height="31" Margin="10,10,0,0" MouseLeftButtonUp="{Binding Path=Settings_Click}"  VerticalAlignment="Top" Width="57" Foreground="#FF2ABD00"/>
                <Label x:Name="LogoutLabel" Content="Logout" HorizontalAlignment="Left" Height="31" Margin="224,10,0,0" MouseLeftButtonUp="{Binding Path=Logout_Click}" VerticalAlignment="Top" Width="54" Foreground="#FF2ABD00"/>
                <Label x:Name="AddFriend_Label" Content="AddFriend" HorizontalAlignment="Left" Height="31" Margin="114,10,0,0" VerticalAlignment="Top" Width="64" Foreground="#FF2ABD00"/>
            </Grid>
        </Border>

    </Grid>
</UserControl>
